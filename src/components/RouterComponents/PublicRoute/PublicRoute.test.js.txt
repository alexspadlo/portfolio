import React from 'react'
import renderer from 'react-test-renderer'
import PublicRoute from './PublicRoute'

describe('Test on <PublicRoute /> component', () => {
	test('Should render LogIn in the path provided', () => {
		const componentNoprops = renderer.create(
			<PublicRoute component={<p>no login</p>} path="/" />
		)
		const treeNoprops = componentNoprops.toJSON()
		expect(treeNoprops).toMatchSnapshot()
	})
})
